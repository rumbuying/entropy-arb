# entropy-arb credentials / 密钥配置
#
#   cp .env.example .env     then fill in / 复制后填写
#
# Required to trade (the bot is live-only). Only --record-only data
# collection runs without credentials.
# 交易必填（本机器人只有实盘模式）。仅 --record-only 数据采集不需要密钥。
# NEVER commit .env / 切勿提交 .env 到 git。

# ===== Entropy — Hyperliquid (always required to trade) =====
# ===== Entropy — Hyperliquid 账户（交易必填） =====
# Create an API/agent wallet at https://app.hyperliquid.xyz/API — put the
# AGENT wallet's private key here and your main account address below.
# 在 https://app.hyperliquid.xyz/API 创建 API (agent) 钱包：
# 这里填 agent 钱包私钥，下面填你的主账户地址。
HL_PRIVATE_KEY=
HL_ACCOUNT_ADDRESS=

# ===== --hedge tradexyz (optional overrides) =====
# tradexyz shares the Hyperliquid account above by default. To trade it from
# a separate account/agent key, set these:
# 对冲腿为 tradexyz 时默认与上面共用同一 Hyperliquid 账户；
# 如需独立账户/密钥，填写以下两项：
#HL_PRIVATE_KEY_XYZ=
#HL_ACCOUNT_ADDRESS_XYZ=

# ===== --hedge lighter or --hedge lighter-rh =====
# ===== 对冲腿为 Lighter 主网或 Lighter Robinhood 链时填写 =====
# Register an API key for the SAME deployment as your --hedge flag
# (mainnet and the Robinhood chain are separate accounts and separate keys).
# See https://github.com/elliottech/lighter-python for key setup.
# 注意：Lighter 主网与 Robinhood 链是两套独立账户和密钥，必须与启动时的
# --hedge 参数一致。密钥生成方法见 lighter-python 官方仓库。
LIGHTER_ACCOUNT_INDEX=
LIGHTER_API_KEY_INDEX=
LIGHTER_API_PRIVATE_KEY=

# ===== per-leg Lighter overrides (only needed when BOTH legs use Lighter) =====
# ===== 两条腿都用 Lighter 时才需要（例如 base=Lighter 主网 + hedge=lighter-rh）=====
# The shared LIGHTER_* triple above is the fallback for whichever leg is on
# Lighter. When the base leg and the hedge leg are on DIFFERENT deployments
# (mainnet vs Robinhood = different accounts and keys), set the leg-specific
# triple here — it wins for that leg only and leaves the other leg on the
# shared triple, so existing workers keep working untouched.
# 上面那组 LIGHTER_* 是两条腿共用的回退值。当基准腿与对冲腿分属不同部署
# （主网 / Robinhood 是两套账户与密钥）时，用下面按腿的变量覆盖：它只对该腿
# 生效，另一条腿仍用共用值，因此不会影响已在跑的 worker。
# e.g. base=Lighter mainnet + hedge=katana needs only LIGHTER_BASE_*:
#LIGHTER_BASE_ACCOUNT_INDEX=
#LIGHTER_BASE_API_KEY_INDEX=
#LIGHTER_BASE_API_PRIVATE_KEY=
#LIGHTER_HEDGE_ACCOUNT_INDEX=
#LIGHTER_HEDGE_API_KEY_INDEX=
#LIGHTER_HEDGE_API_PRIVATE_KEY=

# ===== --hedge katana =====
# ===== 对冲腿为 Katana Perps 时填写 =====
# Create an API key at https://perps.katana.network/wallet/api-keys/login
# with Trade scope. The API secret is shown ONCE — record it as generated.
# The private key is the EOA wallet key that owns the Katana Perps deposit;
# every order is EIP-712-signed with it. KATANA_WALLET is optional (derived
# from the private key unless your setup differs).
# 在 Katana Perps 的 API keys 页面创建密钥（需 Trade 权限）。API secret 仅在
# 创建时显示一次。私钥是充入 Katana Perps 合约的 EOA 钱包私钥，每笔订单都用
# 它做 EIP-712 签名。KATANA_WALLET 可选（默认从私钥推导）。
KATANA_API_KEY=
KATANA_API_SECRET=
KATANA_PRIVATE_KEY=
#KATANA_WALLET=
