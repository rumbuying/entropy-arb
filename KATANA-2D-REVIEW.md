# Katana/BTC 采集 2 天复盘

> 窗口：`2026-09-20T05:14Z → 2026-09-22T07:54Z`（50.7h，3041 分钟）
> 进程：`main.py --config profiles/katana-btc.yaml --symbol BTC --hedge katana --record-only`
> 数据：`logs/minutes-BTC-katana.csv`（唯一产出，未产生任何订单/成交）
> 端点：生产（`api-perps.katana.network`，`KATANA_SANDBOX` 未设置）
> 状态：console worker `w1`，uptime 50.7h，`restarts=0`

---

## 0. TL;DR

- **采集本身满分**：3041 行 / 3041 分钟 = **100% 覆盖，0 缺口**，`samples` 中位数 60，0 重启，生产端点。
- **收获是一个坏消息**：2 天数据**推翻了 MAKER-DESIGN 的核心经济假设**，不是"锁 2bp"，而是
  "付 ~5–8bp 过路费去赌基差收敛"。而且按设计默认参数，**买单 78% 的分钟会被 GTX 拒单**
  （9/22 是 100%）——现在就算开 `maker.enabled: true` 也基本挂不上单。
- `--record-only` 是正确决定：这 2 天基差是**走阔**的（+7.3 → +11.9bp），若已上线做多 KAT/做空 HL，
  现在账面是亏的。

---

## 1. 采集健康度（实测）

| 指标 | 值 |
|---|---|
| 行数 / 跨度 | 3041 行 / 3041 分钟 = **100.00%** |
| 缺口 | **0**（逐分钟连续性检查） |
| `samples` | min 41，p05 59，median 60，<30 的分钟 0 |
| 进程 | uptime 50.7h，restarts 0，console 正常 adopt |
| trades | 无 `trades-BTC-katana.csv`（record-only，符合预期） |

结论：数据可信，可以直接用于统计与回测。**唯一系统性缺口见 §4。**

---

## 2. 市场事实（实测）

### 2.1 溢价（premium = HL mid / KAT mid − 1，单位 bp）

| 日（UTC） | n | mean | std | min | max |
|---|---|---|---|---|---|
| 09-20 | 1126 | **+7.81** | 1.26 | +2.93 | +12.33 |
| 09-21 | 1440 | **+11.10** | 2.53 | +6.62 | +27.74 |
| 09-22 | 475 | **+11.89** | 1.17 | +8.99 | +22.24 |
| 全窗口 | 3041 | **+10.00** | 2.60 | +2.93 | +27.74 |

- 设计文档 §1.1 的基准是 **+7.34bp / std 0.68**（只基于首日 1.5h）。50h 后中枢 **+10.0**、
  **单调上移**，不是文档假设的"结构性、极稳、缓慢衰减"。
- **50h 内 premium 从未 ≤ 0**（最低 +2.93）。基差是**单边、同号、持续**的。

### 2.2 盘口

| | median | mean | p95 |
|---|---|---|---|
| HL(entropy) 价差 | 0.12 bp | 0.13 | 0.12 |
| KAT(hedge) 价差 | **0.70 bp** | 0.88 | 2.23 |

### 2.3 方向性（关键）

以 2 天 3041 分钟逐分钟计算"挂 KAT + 吃 HL"的可成交边际：

| 方向 | 均值 | 为正的分钟占比 |
|---|---|---|
| **买 KAT（挂 bid）/ 卖 HL（吃 bid）** | **+10.37 bp** | **100%** |
| 卖 KAT（挂 ask）/ 买 HL（吃 ask） | −9.61 bp | **0%** |

**两个方向从未同时为正（0/3041 分钟）。** 结论：当前不存在"对称双边报价、库存自然了结"的市场结构——
只有买 KAT 一侧有钱赚，卖 KAT 一侧在 100% 的分钟里都是负的。设计 §3.3 的"对称报价自然了结"在本窗口不成立。

---

## 3. 对 MAKER-DESIGN 的两个硬发现

### 3.1 报价偏移太小 → GTX 会拒单（纯机械问题，与经济学解释无关）

设计 §3.2：`P_bid = hedge_bid / (1 + (C+E)/1e4)`，默认 `C=5.5, E=2.0` → **偏移 7.475bp**。
post-only 合法的充要条件是 **`P_bid < KAT_ask`，等价于 `C+E ≥ sell_edge`**
（`sell_edge = HL_bid/KAT_ask − 1`）。

实测：

| 日 | sell_edge 中位 | 该日 GTX 买单被拒占比 |
|---|---|---|
| 09-20 | 7.40 | 46.2% |
| 09-21 | 9.89 | **96.1%** |
| 09-22 | 11.55 | **100.0%** |
| 全窗口 | 9.23 | **78.2%** |

- 需要的偏移：覆盖 50% / 90% / 99% 的分钟分别是 **9.23 / 12.76 / 16.71 bp**。
- 也就是说，`C+E` 必须**跟着 sell_edge 漂移**；写死 7.475 在当前 regime 下 100% 拒单。
- 反过来，卖单 `P_ask = HL_ask × (1+7.475bp)` 远在 KAT 盘口之外（比 KAT ask 高 ~17bp），
  合法但永远挂着不成交——所以"两边报价"实际退化成"一边持续拒单 + 一边空挂"。

### 3.2 溢价不是挂单收入：两套算法差一个 π

同一笔报价 `P`（买 KAT 成交，同时在 HL bid 对冲），两种算法：

**A. 设计 §3.2 的写法**（把跨所溢价算成收入）：

```
locked_edge = HL_bid/P − 1 − C
            = +10.37 − 5.475 = +4.9 bp     ← 看起来赚钱
```

**B. 逐分钟按两所 mid 计价的真实 MTM**：

```
mtm = (KAT_mid − P) + (HL_bid − HL_mid) − C
```

实测（3041 分钟，全窗口）：

| 报价方式 | 入场 MTM（bp，未扣费） | 扣 C 后 | MTM>0 占比 |
|---|---|---|---|
| 设计默认 `o = C+E = 7.475` | −2.52 | **−8.00** | 0.0% |
| 合法贴盘口 `o = sell_edge + 1 tick` | −0.38 | **−5.86** | 0.0% |
| 直接挂 KAT 买一（排队，不改善价格） | +0.37 | **−5.10** | 0.0% |

两套算法差 **≈ π ≈ 10bp**，正好是溢价本身。原因很直接：
**对冲持仓的 MTM 只看两腿相对各自 mid 的成交价，跨所溢价在两条腿的计价里自动抵消。**
"比 HL 买一低 7.5bp 买入 KAT"并不等于赚 7.5bp，因为 KAT 自己的公允价就在 HL 下方 ~10bp——
按 KAT 自己的盘口算，设计报价 `P` 其实**高于 KAT mid**（在买一之上 2.9bp），是**在付价差，不是在赚价差**。

**这意味着什么**：该策略的真实盈利来源只有一个——**入场后基差收敛**。
这 50h 里基差没有收敛（7.3 → 11.9，走阔），所以它不是一个 lock，而是一个
"付 ~5bp/笔过路费、赌 KAT 追上 HL"的持仓型基差交易，且容量受 KAT 深度限制。

> 注：这两种算法的分歧本质是"跨所溢价是**短暂错价**（会收敛→A 对）还是**长期结构**（不会收敛→B 对）"。
> 2 天数据不能最终判决，但**没有观察到任何收敛力量**：同号、持续、走阔。所以目前证据偏向 B。

---

### 3.3 "价差长期存在" 本身 = 套不动（水平 vs 摆动）

跨所对冲的盈亏**不是**吃到价差的水平，而是价差的**变化量**：

```
买入便宜腿 KAT + 卖空贵腿 HL  →  PnL = −Δπ      （π = HL/KAT − 1）
```

价差恒定 ⇒ 费前盈亏 = 0；你"买到的" 10bp 便宜在平仓时要原样还回去。

实测（3045 分钟）：

| 项 | 值 |
|---|---|
| 整窗持有（买 KAT/卖 HL，π 7.82→13.96） | **−8.19 bp**（费前，已用真实 bid/ask 复算） |
| \|Δπ\| 1min / 1h / 6h / 24h 中位 | 0.60 / 1.06 / 1.49 / **2.82 bp**（p99 ≈ 10–11） |
| 往返成本 all-taker | 13.80 bp |
| 往返成本 KAT 挂单版 | 10.95 bp（其中 HL 吃单 4.5×2 = 9bp） |
| 事后最优单笔（09-21 09:38 π=27.7 → 10:41 π=6.7） | 毛 +17.5 bp → 净 **+3.7 ~ +6.5 bp** |
| 阈值回归 π≥12 开仓 / ≤11 平仓 | 53 次往返，毛 +135 bp，成本 580 bp → **净 −445 bp** |

**常规摆动（2–3bp）盖不住往返成本（11–14bp）；尾部（π≥15 仅占 3.8% 时间）才有肉，
而两天里最好的一笔扣费也就 +4~6bp，且只有事后才知道。**

为什么它长期存在（不是"竞争太激烈"）：perp-perp 没有强制收敛机制——两所资金费都在
+0.01%/8h 下限，净 carry = 0，没有力量把价差拉回；没有现货赎回、没有到期日；KAT 是独立链上
的小所（$2.4M/日、OI 2.3 BTC），资金与用户群割裂，套利资本的边际成本 > 10bp。
类比 A/H 股溢价：**长期存在 ≠ 可以套利**。所以它更像"两个市场的结构性价格水平差"，
而不是"一个等着被捡的错价"。

唯一能翻盘的是**成本**：taker 腿若从 HL(4.5bp) 换成 Lighter(0bp)，往返成本从 11–14bp 降到
~2–5bp ⇒ 24h 中位摆动 2.8bp 已在盈亏平衡附近、5–10bp 的尾部明确为正。**这是最该先验证的方向**
（需实测 KAT-vs-Lighter 的基差与深度）。

> 注意样本局限：这 2 天只有一种 regime（单边、单调走阔、从未 ≤0）。"基差均值回归"这件事
> 本身还没被验证——需要基差收窄/翻转的样本。

---

## 4. 数据缺口（直接影响下一版设计）

1. **recorder 只存 best bid/ask，不存量。** MAKER-DESIGN §1.1 的"±25bp 深度中位 0.10 BTC ≈ $8k"
   是一次性快照，**没有持续监控**；容量/冲击/队列无法从现有数据估计。
2. **只有分钟聚合，没有逐笔/队列。** maker 策略的生死点（fill 概率、adverse selection / markout）
   完全无法从现有数据判断——而 §3.2 的整个结论都建立在"我们会被优先成交"这个未验证假设上。
3. **没有 orders/fills 数据**（record-only），所以 0 条 PnL 证据。
4. `samples` 说明 1s 采样是够的，问题是**采样内容**（只有一档价），不是频率。

---

## 5. 建议（按优先级）

1. **先修偏移，再谈上线。** `maker.edge_bps / costs_bps` 写死会必然拒单。最小改动：
   `o = sell_edge(实时) + 1 tick`，或至少把 `costs_bps + edge_bps` 按最近 24h 的 `sell_edge` P90 设。
   否则 `maker.enabled: true` 的第一次实盘就是一串 `TIME_IN_FORCE`。
2. **把 §3.2 的记账改掉或明确写清假设。** 否则策略会持续给出"净锁 2bp"的假信号，
   而库存阶梯（§3.3）会以为自己在减速一个赚钱的仓位。
3. **成本栈里 4.5bp 的 HL 吃单费占 C 的 82%。** 如果基准腿（taker 腿）能用
   Lighter（0bp）而不是 HL 主网，同样的报价 MTM 从 −5.9 变成 ~−0.1bp——这是把策略从
   "必须靠基差收敛"变成"几乎持平"的最大杠杆。值得单独验证 Lighter BTC 的深度是否够对冲。
4. **先 sandbox 拿 fill 数据**（`KATANA_SANDBOX=1`）：现在最缺的不是分钟价，而是
   成交概率和 markout。没有它，§3.2 无法证伪。
5. **recorder 加一档量（前 N 档 size 之和）**，才能把"容量"变成可监控指标。
6. 若目标是"吃基差收敛"，就把它当**持仓型套利**管理：需要明确的入场基差分位、
   持仓上限、以及能忍受基差走阔的 MTM 预算——这次的 7.3→11.9 就是反面样本。

---

## 附录：复现命令

```bash
# 分布 + 阈值建议
python3 tools/analyze.py --csv logs/minutes-BTC-katana.csv

# 覆盖率/缺口/samples
# （见本文件 §1 脚本：逐分钟连续性 + samples 分布）

# GTX 合法性与入场 MTM
#   sell_edge = entropy_bid/hedge_ask - 1
#   P_bid     = entropy_bid / (1 + (C+E)/1e4)
#   拒单 ⟺ P_bid >= hedge_ask
#   mtm       = ((KAT_mid - P_bid) + (entropy_bid - HL_mid)) / HL_mid * 1e4
```

`C = 0.475 (KAT maker) + 4.5 (HL taker) + 0.5 (滑点) = 5.475`，`E = 2.0`（均为设计与配置默认）。

---

> 后续：跨所（Lighter / Katana / HL）与多标的的探索见 **`BASIS-EXPLORE.md`**。
> 首日结论：真正的杠杆是把对冲腿从 HL(4.5bp) 换成 Lighter(0bp)——往返成本 9.95 → 0.95bp。
