Named strategy profiles managed by the web console (console.py).

  <name>.yaml   a full engine config (same schema as config.yaml)
  <name>.json   sidecar metadata: symbol / hedge for the launch dialog

Profiles contain NO secrets (keys live in .env). Commit them or not —
your choice; they are strategy parameters.
